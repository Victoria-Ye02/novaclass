// =============================================
// main.js — app entry point
// Runs after all other scripts are loaded.
// =============================================

// Start intro animation sequence
runIntro();

// Render gallery slots (loads from localStorage)
initGallery();
